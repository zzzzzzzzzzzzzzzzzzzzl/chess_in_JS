export class rook {
  constructor(colour) {
    this.moved = false
    if (colour == 'b') {
    }
  }
  getValidMoves(board, selected) {
    //input board and selected piece, output an array of valid moves
    const piece = board[selected[0]][selected[2]]
  }
}
